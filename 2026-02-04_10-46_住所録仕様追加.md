セットアップ手順
1. Python環境
powershell# Python 3.13を使用
python --version
# Python 3.13.x

# 仮想環境の確認
# .venv フォルダが存在
2. 必要なパッケージのインストール
powershellpip install django
pip install mysqlclient
pip install openpyxl  # Excel操作用（将来の拡張用）
```

---

## 📂 プロジェクトの作成

### ディレクトリ構造
```
PythonProject/
├── .venv/                          # 仮想環境
│   ├── Lib/
│   ├── Scripts/
│   └── ...
└── addressbook_project/            # Djangoプロジェクト
    ├── addressbook_project/        # 設定フォルダ
    │   ├── __init__.py
    │   ├── settings.py             # プロジェクト設定
    │   ├── urls.py                 # メインURLルーティング
    │   ├── asgi.py
    │   └── wsgi.py
    ├── contacts/                   # 住所録アプリ
    │   ├── migrations/             # データベースマイグレーション
    │   ├── templates/              # HTMLテンプレート
    │   │   └── contacts/
    │   │       ├── base.html
    │   │       ├── contact_list.html
    │   │       ├── contact_detail.html
    │   │       ├── contact_form.html
    │   │       └── contact_confirm_delete.html
    │   ├── __init__.py
    │   ├── admin.py                # 管理サイト設定
    │   ├── apps.py                 # アプリ設定
    │   ├── models.py               # データモデル
    │   ├── views.py                # ビュー（処理）
    │   ├── forms.py                # フォーム定義
    │   ├── urls.py                 # URLルーティング
    │   └── tests.py
    ├── manage.py                   # Django管理コマンド
    └── db.sqlite3                  # 旧データベース（現在は未使用）
プロジェクト作成の流れ
powershell# 1. Djangoプロジェクトを作成
django-admin startproject addressbook_project

# 2. プロジェクトディレクトリに移動
cd addressbook_project

# 3. contactsアプリを作成
python manage.py startapp contacts

# 4. テンプレートフォルダを作成
mkdir contacts\templates\contacts
🎨 アプリケーションの機能
1. データモデル（models.py）
連絡先を表すContactモデル:
pythonclass Contact(models.Model):
    last_name = models.CharField('姓', max_length=50)
    first_name = models.CharField('名', max_length=50)
    last_name_kana = models.CharField('姓（フリガナ）', max_length=50, blank=True)
    first_name_kana = models.CharField('名（フリガナ）', max_length=50, blank=True)
    phone_number = models.CharField('電話番号', max_length=20, blank=True)
    email = models.EmailField('メールアドレス', blank=True)
    postal_code = models.CharField('郵便番号', max_length=10, blank=True)
    address = models.CharField('住所', max_length=200, blank=True)
    notes = models.TextField('メモ', blank=True)
    created_at = models.DateTimeField('作成日時', auto_now_add=True)
    updated_at = models.DateTimeField('更新日時', auto_now=True)
2. ビュー（views.py）
5つのクラスベースビュー:

ContactListView: 一覧表示と検索
ContactDetailView: 詳細表示
ContactCreateView: 新規作成
ContactUpdateView: 編集
ContactDeleteView: 削除

3. フォーム（forms.py）

ContactForm: 連絡先の入力フォーム
ContactSearchForm: 検索フォーム

4. テンプレート

base.html: 共通レイアウト
contact_list.html: 一覧ページ
contact_detail.html: 詳細ページ
contact_form.html: 作成・編集ページ
contact_confirm_delete.html: 削除確認ページ


🎨 カスタマイズ内容
デザインのカスタマイズ
1. グリーンテーマ

ナビゲーションバー: ダークグリーン（#2d6a4f）
ボタン: ライトグリーン（#52b788）
背景: ライトグリーンベージュ（#f0f8f5）

2. フォント

Googleフォント「Noto Sans JP」を使用
読みやすく、日本語に最適化

3. レイアウト

Bootstrap 5を使用
レスポンシブデザイン
モバイルフレンドリー

機能のカスタマイズ
1. フリガナ項目の追加

姓・名のフリガナフィールド
検索にも対応

2. 複数キーワード検索

スペース区切りで複数キーワード検索可能
名前、フリガナ、電話番号、メール、住所を検索対象

3. フォントウェイト調整

連絡先名を太く（font-weight: 700）
ボタンは標準（font-weight: 400）


🗄️ データベース設定
MySQL設定
1. データベースの作成
sqlCREATE DATABASE addressbook CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
2. Django設定（settings.py）
pythonDATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'addressbook',
        'USER': 'root',
        'PASSWORD': 'your_password',
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'charset': 'utf8mb4',
            'init_command': "SET sql_mode='STRICT_TRANS_TABLES'",
        },
    }
}
3. マイグレーション
powershellpython manage.py makemigrations
python manage.py migrate
データベーステーブル
作成されたテーブル:

contacts_contact: 連絡先データ
auth_*: 認証関連
django_*: Django管理用


📋 重要な設定ファイル
settings.py の主要設定
python# 言語とタイムゾーン
LANGUAGE_CODE = 'ja'
TIME_ZONE = 'Asia/Tokyo'

# インストール済みアプリ
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'contacts',  # 住所録アプリ
]
URLルーティング
プロジェクトURL（addressbook_project/urls.py）
pythonurlpatterns = [
    path('admin/', admin.site.urls),
    path('contacts/', include('contacts.urls')),
    path('', RedirectView.as_view(url='/contacts/', permanent=True)),
]
アプリURL（contacts/urls.py）
pythonurlpatterns = [
    path('', views.ContactListView.as_view(), name='contact-list'),
    path('create/', views.ContactCreateView.as_view(), name='contact-create'),
    path('<int:pk>/', views.ContactDetailView.as_view(), name='contact-detail'),
    path('<int:pk>/update/', views.ContactUpdateView.as_view(), name='contact-update'),
    path('<int:pk>/delete/', views.ContactDeleteView.as_view(), name='contact-delete'),
]

🚀 アプリケーションの起動方法
1. サーバーの起動
powershell# addressbook_projectディレクトリで
cd C:\Users\nichibiWin03\PycharmProjects\PythonProject\addressbook_project

# サーバー起動
python manage.py runserver
2. アクセス
ブラウザで以下のURLを開く:

住所録トップ: http://127.0.0.1:8000/contacts/
管理サイト: http://127.0.0.1:8000/admin/

3. サーバーの停止
ターミナルで Ctrl+C

🔧 今後の開発方法
新しい連絡先を追加

ブラウザで http://127.0.0.1:8000/contacts/ を開く
「新しい連絡先を追加」ボタンをクリック
フォームに情報を入力
「追加」ボタンをクリック

コードの編集
モデルを変更する場合
powershell# 1. models.pyを編集
# 2. マイグレーションを作成
python manage.py makemigrations

# 3. データベースに適用
python manage.py migrate
デザインを変更する場合

contacts/templates/contacts/base.html の <style> タグ内を編集
ブラウザをリロード（F5）

機能を追加する場合

models.py にフィールドを追加
forms.py にフォームフィールドを追加
views.py に処理を追加
テンプレートを編集
マイグレーションを実行


🔍 トラブルシューティング
サーバーが起動しない
powershell# 現在のディレクトリを確認
pwd
# addressbook_projectにいることを確認

# ポートが使用中の場合
python manage.py runserver 8001
データベースエラー
powershell# マイグレーションを再実行
python manage.py migrate

# MySQLに接続できるか確認
mysql -u root -p
テンプレートが見つからない
powershell# ディレクトリ構造を確認
dir contacts\templates\contacts
